package com.example.bakery.controller.entities;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("padaria")
public class LucroPorPeriodoController {

    /*
        @Autowired
        ItemVendaRepository itemVendaRepository;
        @GetMapping("lucro-por-periodo")
        public List<LucroPorPeriodoDTO> getLucroPorPeriodo(@RequestParam("date1") String date1, @RequestParam("date2") String date2){
            return itemVendaRepository.getLucroPorPeriodo(date1, date2);
        }
     */
}
